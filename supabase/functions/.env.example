# Edge function secrets (local). Do not commit real keys.
# Copy from Stripe Dashboard (test mode) and from `npx supabase status`.

STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
SITE_URL=http://localhost:3000
